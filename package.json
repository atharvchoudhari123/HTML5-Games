{
  "name": "arcade-lounge",
  "version": "1.0.0",
  "description": "Local offline host for my single-file HTML arcade games",
  "main": "index.html",
  "scripts": {
    "start": "live-server --entry-file=index.html --port=3000"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}